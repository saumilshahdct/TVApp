<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto"
	xmlns:tools="http://schemas.android.com/tools"
	tools:context=".feature.routing.ui.RoutingScreen">

	<data>

		<import type="android.view.View" />

		<variable
			name="routing"
			type="com.veeps.app.feature.routing.viewModel.RoutingViewModel" />

		<variable
			name="routingScreen"
			type="com.veeps.app.feature.routing.ui.RoutingScreen" />

	</data>

	<androidx.constraintlayout.widget.ConstraintLayout
		android:id="@+id/container"
		android:layout_width="match_parent"
		android:layout_height="match_parent">

		<include layout="@layout/guidelines" />

		<!-- Logo -->
		<ImageView
			android:id="@+id/logo"
			android:layout_width="0dp"
			app:layout_constraintDimensionRatio="1:1.5"
			android:layout_height="0dp"
			android:contentDescription="@string/app_description"
			android:src="@drawable/logo"
			app:layout_constraintBottom_toBottomOf="@+id/bottom"
			app:layout_constraintEnd_toEndOf="@+id/end"
			app:layout_constraintStart_toStartOf="@+id/start"
			app:layout_constraintTop_toTopOf="@+id/top" />

		<androidx.core.widget.ContentLoadingProgressBar
			android:id="@+id/loader"
			style="?android:attr/progressBarStyleLarge"
			android:layout_width="50dp"
			android:layout_height="50dp"
			android:indeterminateTint="@color/white"
			android:progress="50"
			app:layout_constraintBottom_toBottomOf="@+id/bottom"
			app:layout_constraintEnd_toEndOf="@+id/end"
			app:layout_constraintStart_toStartOf="@+id/start"
			app:layout_constraintTop_toBottomOf="@id/logo" />

		<!-- Error Container -->
		<androidx.constraintlayout.widget.ConstraintLayout
			android:id="@+id/error_container"
			android:layout_width="match_parent"
			android:layout_height="match_parent"
			android:background="@color/dark_black_90">

			<include layout="@layout/guidelines" />

			<!-- Error -->
			<TextView
				android:id="@+id/error"
				android:layout_width="wrap_content"
				android:layout_height="wrap_content"
				android:layout_marginTop="10dp"
				android:layout_marginBottom="10dp"
				android:padding="10dp"
				android:text="@{routing.errorMessage}"
				android:textColor="@color/white"
				android:textIsSelectable="false"
				android:textSize="16sp"
				app:layout_constrainedWidth="true"
				app:layout_constraintBottom_toTopOf="@+id/error_action_container"
				app:layout_constraintEnd_toEndOf="@+id/post_quarter_width"
				app:layout_constraintStart_toStartOf="@+id/pre_quarter_width"
				app:layout_constraintTop_toTopOf="@+id/top"
				app:layout_constraintVertical_chainStyle="packed" />

			<!-- Error CTA Container -->
			<androidx.constraintlayout.widget.ConstraintLayout
				android:id="@+id/error_action_container"
				android:layout_width="0dp"
				android:layout_height="wrap_content"
				android:layout_marginTop="10dp"
				android:layout_marginBottom="10dp"
				android:padding="10dp"
				app:layout_constraintBottom_toBottomOf="@+id/bottom"
				app:layout_constraintEnd_toEndOf="@+id/error"
				app:layout_constraintStart_toStartOf="@+id/error"
				app:layout_constraintTop_toBottomOf="@+id/error">

				<Button
					android:id="@+id/positive"
					fontPath="fonts/GothamMedium.ttf"
					android:layout_width="0dp"
					android:layout_height="wrap_content"
					android:backgroundTint="@color/white"
					android:contentDescription="@string/app_description"
					android:focusable="true"
					android:focusableInTouchMode="true"
					android:lines="1"
					android:onClick="@{(v)-> routingScreen.onErrorPositive(v.tag)}"
					android:text="@{routing.errorPositiveLabel}"
					android:textIsSelectable="false"
					android:textSize="12sp"
					android:textColor="@color/dark_black"
					android:visibility="@{routing.isErrorPositiveApplicable() ? View.VISIBLE : View.GONE}"
					app:layout_constraintBottom_toBottomOf="parent"
					app:layout_constraintEnd_toStartOf="@+id/negative"
					app:layout_constraintHorizontal_chainStyle="packed"
					app:layout_constraintStart_toStartOf="parent"
					app:layout_constraintTop_toTopOf="parent" />

				<Button
					android:id="@+id/negative"
					fontPath="fonts/GothamMedium.ttf"
					android:layout_width="0dp"
					android:layout_height="wrap_content"
					android:backgroundTint="@color/white"
					android:textColor="@color/dark_black"
					android:contentDescription="@string/app_description"
					android:focusable="true"
					android:focusableInTouchMode="true"
					android:lines="1"
					android:onClick="@{(v)-> routingScreen.onErrorNegative(v.tag)}"
					android:text="@{routing.errorNegativeLabel}"
					android:textIsSelectable="false"
					android:textSize="12sp"
					android:visibility="@{routing.isErrorNegativeApplicable() ? View.VISIBLE : View.GONE}"
					app:layout_constraintBottom_toBottomOf="parent"
					app:layout_constraintEnd_toEndOf="parent"
					app:layout_constraintStart_toEndOf="@+id/positive"
					app:layout_constraintTop_toTopOf="parent" />

			</androidx.constraintlayout.widget.ConstraintLayout>

		</androidx.constraintlayout.widget.ConstraintLayout>

	</androidx.constraintlayout.widget.ConstraintLayout>

</layout>
